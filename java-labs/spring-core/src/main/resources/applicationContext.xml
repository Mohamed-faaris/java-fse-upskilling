<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/aop
           https://www.springframework.org/schema/aop/spring-aop.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Enable Spring AOP -->
    <aop:aspectj-autoproxy/>

    <!-- Component Scan: discovers @Service, @Repository, @Component, etc. -->
    <context:component-scan base-package="org.library"/>

    <!-- Aspect Bean (no annotation on LoggingAspect yet) -->
    <bean id="loggingAspect"
          class="org.library.aspect.LoggingAspect"/>

    <!-- AOP Configuration -->
    <aop:config>

        <aop:aspect ref="loggingAspect">

            <!-- Apply to all methods in service package -->
            <aop:pointcut
                id="serviceMethods"
                expression="execution(* org.library.service.*.*(..))"/>

            <!-- Before Advice -->
            <aop:before
                method="beforeAdvice"
                pointcut-ref="serviceMethods"/>

            <!-- After Advice -->
            <aop:after
                method="afterAdvice"
                pointcut-ref="serviceMethods"/>

        </aop:aspect>

    </aop:config>

</beans>